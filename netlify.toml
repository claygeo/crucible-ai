[build]
  command = "npm run build"
  publish = ".next"

# Next.js runtime is auto-detected by Netlify; no need to declare the plugin
# explicitly here (and declaring it conflicts with auto-install in some flows).

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit --no-fund"
