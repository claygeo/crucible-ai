[build]
  command = "npm run build"
  # Let Netlify auto-detect the publish dir for Next.js.
  # The OpenNext adapter handles .next, server runtime, edge funcs.

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--no-audit --no-fund"
